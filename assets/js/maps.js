// Generated by CoffeeScript 1.6.3
(function() {
  var mod;

  mod = angular.module('btMaps', []);

  mod.directive('leafletMap', function() {
    var func;
    func = function(scope, element, attrs) {
      var center, id, lat, lng, zoom;
      id = attrs.mapId;
      center = attrs.latLng.split(",");
      lat = center[0];
      lng = center[1];
      zoom = center[2];
      if ((zoom == null) || zoom === "") {
        zoom || (zoom = 8);
      }
      return L.mapbox.map(id, 'xrd.igb6dabo').setView([lat, lng], zoom);
    };
    return func;
  });

  angular.module('bt', ['btMaps']);

}).call(this);
